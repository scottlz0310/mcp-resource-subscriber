{
  "name": "mcp-resource-subscriber",
  "version": "0.6.1",
  "description": "CLI probe for MCP resource subscriptions (protocol revision 2026-07-28) — open a subscriptions/listen stream, receive live update notifications, and re-read updated content.",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "subscriptions-listen",
    "mcp-client",
    "cli",
    "probe",
    "notifications"
  ],
  "homepage": "https://github.com/scottlz0310/mcp-resource-subscriber",
  "type": "module",
  "packageManager": "pnpm@12.4.2",
  "license": "MIT",
  "bin": {
    "mcp-resource-subscriber": "dist/src/client/cli.js"
  },
  "files": [
    "dist/src/client/",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/scottlz0310/mcp-resource-subscriber.git"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server/index.js",
    "dev": "tsx src/server/index.ts",
    "probe:subscribe": "tsx scripts/subscribe-client.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check": "biome check .",
    "format": "biome format --write .",
    "prepare": "lefthook install || true"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/core": "^2.0.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "@types/express": "^5.0.6",
    "@types/node": "^26.6.2",
    "@vitest/coverage-v8": "^5.0.1",
    "express": "^5.2.1",
    "lefthook": "^2.1.14",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  },
  "engines": {
    "node": ">=26.9.0"
  }
}
